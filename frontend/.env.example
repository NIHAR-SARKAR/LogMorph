# Frontend dev server
VITE_PORT=3000
VITE_ALLOWED_HOSTS=localhost

# Backend proxy target
VITE_BACKEND_URL=http://localhost:8000
VITE_PROXY_SECURE=false

# SSL (optional — set both key + cert paths to enable HTTPS on the dev server)
VITE_SSL_KEY_FILE=
VITE_SSL_CERT_FILE=
